import 'package:flutter/material.dart';

class ConditonScreen extends StatelessWidget {
  const ConditonScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Condition'),
      ),
    );
  }
}
